# 2026-04-13

## What I Did

- Hardened the repository for long-term AI collaboration.
- Added bilingual top-level Markdown counterparts for repository guidance files.
- Added handoff, start entrypoint, user profile, collaboration, asking, growth, QA, and conversation history documents.
- Formalized the rule that every Markdown document must exist as both `.md` and `.ja.md`.
- Updated the journal helper script to create both English and Japanese journal files.
- Saved additional conversation context and prompt-improvement feedback into repository documents.
- Expanded `exercises/docker/02-nginx` README files with pre-run checks, post-run verification, and a layer-by-layer troubleshooting order.
- Updated `STATUS.*` and `HANDOFF.*` so the next local command is explicit for resume.
- Updated stale latest-journal references in `README.*`.
- Added the learning question "What is nginx?" to the `02-nginx` exercise README and the repository-wide Q&A.
- Added the Japanese pronunciation of `nginx` as `Engine-X` (`エンジンエックス`) to the `02-nginx` exercise README and the repository-wide Q&A.
- Added an explanation of why `docker run nginx:alpine` shows different output on the first run versus later runs, focusing on image pull and local cache behavior.
- Added an explanation of what "stored locally" means, including the Docker Desktop `Docker.raw` disk image on macOS and the `data-root` concept on Linux.
- Added an explanation of what it means for Docker data to be "inside `Docker.raw`", using the virtual disk and Linux filesystem model.
- Added a Q&A note that Docker Desktop on macOS uses a Docker-managed Linux VM and that CPU, memory, and disk are controlled through Docker Desktop resource settings.
- Added a Q&A note that `-v` supports both file mounts and directory mounts, and that mounting the whole content directory is the default pattern for multi-file static sites (HTML + CSS + JS).
- Added a learning pattern to visualize both host-side port (observed from URL) and container-side port (shown as config value) in `02-nginx` `index.html` and related Q&A notes.
- Added a Q&A note explaining how to confirm `-v` host/container path mappings using `docker inspect` `Mounts` and `docker exec` from inside the container.
- Added a Q&A note explaining how to find a container ID with `docker ps`, when to use names instead, and how `--name` and `docker ps -q` fit into the workflow.
- Added a Q&A note clarifying the difference between `docker ps` `IMAGE` and `NAMES`: the image is the source blueprint, while the name identifies the running container instance.
- Added a Q&A note listing the standard day-to-day Docker commands to learn first, including `logs`, `inspect`, `exec`, `images`, `run`, and `stop`.
- Added a Q&A note clarifying that `docker images` `In Use` does not always mean an image is currently running; stopped containers can still keep an image marked as in use.
- Added a Q&A note showing how to remove stopped `hello-world` containers both one by one and in a single filtered command.
- Added a Q&A note clarifying that `docker exec` requires a running container and that bind-mounted file state is what `docker exec` sees.
- Added a mastery checklist to `02-nginx` so readiness to move to Exercise 03 can be judged objectively.
- Moved learning focus to Exercise 03 (`exercises/docker/03-custom-nginx`) and updated `STATUS.*` with concrete build/run next actions.

## What I Learned

- A strong AI collaboration workflow benefits from explicit handoff files, repository policy, and question indexing.
- Conversation quality improves further when prompt patterns are also preserved as reusable knowledge.
- Bilingual documentation rules reduce ambiguity and make future sessions easier to resume.
- Even one `docker run` exercise is enough to practice separating host, container, and published-port responsibilities.

## What Was Confusing

- The repository had grown quickly enough that some guidance files needed clearer indexing and pairing rules.
- This sandbox cannot connect to the Docker socket, so live verification still depends on running the command on the user's machine.

## Next Step

- Run `docker build -t my-custom-nginx:latest .` in `exercises/docker/03-custom-nginx`
- Run `docker run --rm -p 8080:80 my-custom-nginx:latest` and verify the page
- Explain Exercise 02 vs 03: immediate reflection with bind mount vs rebuild-required with image build
- Record new questions into both local exercise Q&A and repository-wide Q&A
- Keep conversation-history entries up to date when new operating patterns emerge
