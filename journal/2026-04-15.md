# 2026-04-15

## Current Status
Started a new AI session. Read the handoff and policy documents including `START_HERE.md` and successfully grasped the current context.
The current focus is on `exercises/docker/03-custom-nginx`. Up to the last session, we've advanced our understanding of the `docker build` context, file permissions, and `COPY` semantics through error diagnosis.

## Today's Plan
1. Run the following command in `exercises/docker/03-custom-nginx` to build the image:
   `docker build -t my-custom-nginx:latest .`
2. Start the container from the built image and verify in the browser:
   `docker run --rm -p 8080:80 my-custom-nginx:latest`
3. Document and verbalize the difference between Exercise 02 (bind mount/immediate reflection) and Exercise 03 (build/baking image).

## Work Log
- Encountered `failed to connect to the docker API at unix:///Users/.../docker.sock` error when running `docker build`.
- Explained that this is caused by the Docker daemon (server) not running, leaving the CLI (client) unable to communicate.
- Added a Q&A entry to conceptually explain Docker's client-server architecture using this real-world error scenario.
- Responded to user's question on how to check daemon status from the terminal, explaining the use of `docker version` and `docker info` to perform a client-server health check, and recorded it in Q&A.
- The user successfully built the custom image and started the container.
- The user perfectly articulated the architectural difference between Exercise 02 (bind mount = receiving required files from the outside) and Exercise 03 (build = constructing an image with the files baked inside). Exercise 03 is now marked complete.
- Based on user feedback noting the discovery of `k3s`, added the concept of "Kubernetes Distributions" to the Day 5 curriculum (`README` and QA index), as understanding implementations vs. the API standard is highly relevant for support engineering.
- Progressing to the next phase: Docker Compose (Exercise 04).
- The user successfully executed `docker compose up -d` and `down` locally, observing container orchestration via the YAML file.
- The user perfectly summarized Docker Compose as "a mechanism that extracts the option sequences (like `-p`, `-v`) previously written in `docker run` into a `compose.yaml` file, and uses the command to read it and start processes". Practical understanding of IaC fundamentals is achieved, marking the end of the Docker basics and Compose phase. The study now advances to Kubernetes Fundamentals.
- The user exhibited a deep analytical understanding by questioning how relative paths in `volumes` are resolved when executing `docker compose` from a different directory using `-f`. Explained that relative paths are always resolved relative to the compose file itself ensuring robust reproducibility regardless of execution location, and recorded this in Q&A.
- The user asked an advanced architectural question about running a Docker container inside another Docker container (DinD/DooD). Explained that this is exactly the foundational concept behind the upcoming tool `kind` (Kubernetes in Docker), and recorded the insight in the Q&A.
