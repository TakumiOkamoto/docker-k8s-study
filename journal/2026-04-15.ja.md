# 2026-04-15

## 現在地
新しい AI セッションを開始しました。`START_HERE.ja.md` 等の各種引き継ぎ・ポリシー文書を読み込み、現在の状況を把握しました。
現在の進捗の焦点は `exercises/docker/03-custom-nginx` です。前回までに `docker build` 時の context や権限管理、`COPY` に関するエラー診断など理論と実践を進めています。

## 本日の予定
1. `exercises/docker/03-custom-nginx` ディレクトリで以下のコマンドを実行し、イメージをビルドする。
   `docker build -t my-custom-nginx:latest .`
2. 作成したイメージからコンテナを起動し、ブラウザで確認する。
   `docker run --rm -p 8080:80 my-custom-nginx:latest`
3. 前回学んだ演習 02（bind mount による即時反映）と、演習 03（build によるイメージ化）の違いについて言語化・整理する。

## 作業記録
- `docker build` 実行時に `failed to connect to the docker API at unix:///Users/.../docker.sock` エラーが発生。
- これは Docker デーモン（サーバー）が起動していないために、CLI（クライアント）が通信できないことが原因。
- Docker の「クライアント・サーバーモデル」の構造を学ぶ良いエラーケースとして、Q&A を追加した。
- ユーザーから「ターミナル上でデーモン状態を確認できるか」質問があり、`docker version` と `docker info` でのクライアント側からの疎通確認方法を Q&A に追記した。
- ユーザーが無事に独自イメージをビルドし、コンテナを起動した。
- ユーザーが「02 (bind mount = 外から受け入れる前提) と 03 (build = 中に入れ込んだイメージの構築) の違い」を完璧に言語化したため、本演習完了とした。
- ユーザーからのフィードバックにより、`k3s` という存在について学習し、サポート視点で重要な「Kubernetes ディストリビューション」の概念として Day 5 のカリキュラム（`README` 及び QA インデックス）に追加した。
- 次のフェーズである Docker Compose（演習04）の準備に進んだ。
- ユーザーの手元で `docker compose up -d` や `down` 等の一連の起動制御が成功した。
- 「長かった `docker run` のオプション列（`-p` や `-v`）をファイル `compose.yaml` として抽出・定義し、それを読み込んで起動する仕組み」と完璧な言語化ができ、IaC の実用性を体感した。これにより Docker 基礎〜Compose フェーズが完了。Kubernetes 基礎（Day 5）へと歩みを進めた。
