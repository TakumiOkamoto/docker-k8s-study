# 2026-05-03

## 現在の状況
`START_HERE.md` からセッション再開。Docker と Kubernetes ツールの可用性を確認。`study` という名前のローカル `kind` クラスタを正常に作成。ノードステータスが `Ready` でクラスタが動作中。

## 今日の計画
1. 最初の Kubernetes deployment を適用: `kubectl apply -f exercises/k8s/01-deployment/deployment.yaml`
2. Pod 作成を確認: `kubectl get pods`
3. Pod 詳細を検査: `kubectl describe pod <pod-name>`
4. ログを確認: `kubectl logs <pod-name>`
5. クリーンアップ: `kubectl delete -f exercises/k8s/01-deployment/deployment.yaml`

## 作業ログ
- `kind` と `kubectl` のバージョンを確認
- `kind create cluster --name study` を実行（クラスタ作成成功）
- クラスタステータスを確認: `kubectl get nodes` で `study-control-plane` が `Ready` と表示
- 現在のコンテキスト: `kind-study`
- クラスタ情報: API サーバーが `https://127.0.0.1:61458` で動作
- 2つのクラスタが存在: `study` と `study-cluster`（以前のセッションの可能性）
- `notes/10-k8s-basics.md` に kubeconfig の説明を追加し、QA 索引を更新