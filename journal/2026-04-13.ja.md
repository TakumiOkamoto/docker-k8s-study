# 2026-04-13

## やったこと

- repository を長期的な AI 協働向けに強化した
- repository ガイド系ファイルの英日ペアを追加した
- handoff、開始入口、利用者プロファイル、協働ガイド、聞き方ガイド、成長ガイド、Q&A、会話履歴を整備した
- 全ての Markdown を `.md` と `.ja.md` のペアで持つルールを明文化した
- journal 生成スクリプトを英日両方のファイルを作るように更新した
- 追加の会話文脈と、より良い入力案を repository に保存した
- `exercises/docker/02-nginx` の README を補強し、実行前チェック、実行後確認、レイヤー別切り分け順を追加した
- `STATUS.*` と `HANDOFF.*` を更新し、次に利用者がローカルで実行すべき具体的コマンドを明示した
- `README.*` の古い直近日報参照を最新日に合わせて更新した
- `nginx` とは何か、という学習質問を `02-nginx` 演習 README と全体 Q&A に追記した
- `nginx` の日本語での発音 `エンジンエックス` を `02-nginx` 演習 README と全体 Q&A に追記した

## 学んだこと

- 強い AI 協働には、handoff、repository policy、Q&A 索引が効く
- プロンプト改善の知見自体を記録すると、将来の会話品質も上げやすい
- バイリンガル文書ルールは、再開時の迷いを減らす
- `docker run` 1 本でも、ホスト、コンテナ、ポート公開の 3 層を切り分けて考える練習にできる

## 混乱したこと

- repository が急速に育ったため、ガイド系ファイルの索引とペア運用ルールをより明確にする必要があった
- この sandbox からは Docker ソケットに接続できず、live 確認は利用者の手元実行に依存する

## 次にやること

- `exercises/docker/02-nginx` で `docker run --rm -p 8080:80 -v "$PWD/index.html:/usr/share/nginx/html/index.html:ro" nginx:alpine` を実行する
- `localhost:8080` で表示確認する
- ホスト、コンテナ、ポート公開のどこに責務があるかを言葉で整理する
- 新しい質問を演習側 Q&A と repository 全体 Q&A の両方に残す
- 新しい運用パターンが出たら会話履歴も更新する
