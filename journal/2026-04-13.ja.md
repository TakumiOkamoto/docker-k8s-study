# 2026-04-13

## やったこと

- repository を長期的な AI 協働向けに強化した
- repository ガイド系ファイルの英日ペアを追加した
- handoff、開始入口、利用者プロファイル、協働ガイド、聞き方ガイド、成長ガイド、Q&A、会話履歴を整備した
- 全ての Markdown を `.md` と `.ja.md` のペアで持つルールを明文化した
- journal 生成スクリプトを英日両方のファイルを作るように更新した
- 追加の会話文脈と、より良い入力案を repository に保存した
- `exercises/docker/02-nginx` の README を補強し、実行前チェック、実行後確認、レイヤー別切り分け順を追加した
- `STATUS.*` と `HANDOFF.*` を更新し、次に利用者がローカルで実行すべき具体的コマンドを明示した
- `README.*` の古い直近日報参照を最新日に合わせて更新した
- `nginx` とは何か、という学習質問を `02-nginx` 演習 README と全体 Q&A に追記した
- `nginx` の日本語での発音 `エンジンエックス` を `02-nginx` 演習 README と全体 Q&A に追記した
- 初回実行と二度目以降で `docker run nginx:alpine` の出力が違う理由を、イメージ pull とローカルキャッシュの観点で `02-nginx` 演習 README と全体 Q&A に追記した
- `ローカルに image がある` の意味を、Docker Desktop on macOS の `Docker.raw` と Linux の `data-root` の違いを含めて `02-nginx` 演習 README と全体 Q&A に追記した
- `Docker.raw` の中にある、の意味を仮想ディスクと Linux filesystem の関係で `02-nginx` 演習 README と全体 Q&A に追記した
- Docker Desktop on macOS の Linux VM が Docker Desktop 管理下にあり、Resources 設定で CPU / memory / disk を管理することを Q&A に追記した
- `-v` が file mount と directory mount の両方に使えること、HTML + CSS + JS のような複数ファイル構成では配信ディレクトリごと mount するのが基本であることを `02-nginx` 演習 README と全体 Q&A に追記した
- HTML 上で host 側ポート（URL から実測）と container 側ポート（設定値）を並べて可視化する学習パターンを `02-nginx` の `index.html` と Q&A に追記した
- `-v` でつないだ host 側 path と container 側 path を `docker inspect` の `Mounts` と `docker exec` で確認する方法を `02-nginx` の README と全体 Q&A に追記した
- `container-id` の確認方法として `docker ps`、`NAMES`、`--name`、`docker ps -q` の使い分けを `02-nginx` の README と全体 Q&A に追記した
- `docker ps` の `IMAGE` と `NAMES` の違いを、image は元データ、name は起動中 container 実体の識別子という観点で `02-nginx` の README と全体 Q&A に追記した
- Docker で日常的によく使う基本コマンド群として `docker logs`、`inspect`、`exec`、`images`、`run`、`stop` などを `02-nginx` の README と全体 Q&A に追記した
- `docker images` の `In Use` / `U` が「今動いている」とは限らず、停止済み container が image を参照していても付くことを、実際の `hello-world` と `nginx:alpine` の状態確認付きで `02-nginx` の README と全体 Q&A に追記した
- 停止済み `hello-world` container の削除方法を、1個ずつ消す手順とまとめて消す手順の両方で `02-nginx` の README と全体 Q&A に追記した
- `docker exec` が running container 前提であることと、bind mount 後のファイル状態が `docker exec` で見えることを `02-nginx` README と全体 Q&A に追記した
- `02-nginx` に学習完了チェックリストを追加し、03 に進む判断基準を明確化した
- 学習フェーズを 03 (`exercises/docker/03-custom-nginx`) へ移行し、次に実行する build / run 手順を `STATUS.*` に明示した
- `docker build -t my-custom-nginx:latest .` の `.` が build context（Docker に渡すファイル範囲）を意味することを `03-custom-nginx` README と全体 Q&A に追記した
- `docker build -t my-custom-nginx:latest -f ../Dockerfile` で発生した `requires 1 argument` の原因（`-f` とは別に context 引数が必須）を `03-custom-nginx` README と全体 Q&A に追記した
- `COPY index.html ...` の `"/index.html": not found` は context にファイルが無いことが原因で、`transferring context: 2B` は context ほぼ空のサインであることを `03-custom-nginx` README と全体 Q&A に追記した
- `transferring context: 2B` の `2B` は build context の容量（2 bytes）であることを `03-custom-nginx` README と全体 Q&A に追記した
- build 対象ファイルが散在する場合の方針（repo ルート context + `.dockerignore`、staging copy、BuildKit `--build-context`）と、symlink より copy 集約を推奨する理由を `03-custom-nginx` README と全体 Q&A に追記した
- context 内のファイルをコンテナ内の別々の配置先へ展開できること（`COPY <src> <dest>` を複数指定）を `03-custom-nginx` README と全体 Q&A に追記した

## 学んだこと

- 強い AI 協働には、handoff、repository policy、Q&A 索引が効く
- プロンプト改善の知見自体を記録すると、将来の会話品質も上げやすい
- バイリンガル文書ルールは、再開時の迷いを減らす
- `docker run` 1 本でも、ホスト、コンテナ、ポート公開の 3 層を切り分けて考える練習にできる

## 混乱したこと

- repository が急速に育ったため、ガイド系ファイルの索引とペア運用ルールをより明確にする必要があった
- この sandbox からは Docker ソケットに接続できず、live 確認は利用者の手元実行に依存する

## 次にやること

- `exercises/docker/03-custom-nginx` で `docker build -t my-custom-nginx:latest .` を実行する
- `docker run --rm -p 8080:80 my-custom-nginx:latest` を実行して表示確認する
- 演習 02 と 03 の違い（即時反映 vs 再ビルド必要）を言語化する
- 新しい質問を演習側 Q&A と repository 全体 Q&A の両方に残す
- 新しい運用パターンが出たら会話履歴も更新する

## サマリー

Exercise 03 の進行：エラー診断から複数宛先へのファイル配置パターンまで理解した。Docker build 時のファイル権限制御を習得：デフォルト動作、`COPY --chown` 方法、`RUN chmod/chown` 指針。`docker build` の理論的理解から、散在入力と複数宛先展開、権限管理の実践的処理へ進んだ。
