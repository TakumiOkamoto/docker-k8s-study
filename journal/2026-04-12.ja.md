# 2026-04-12

## やったこと

- 学習用 repository を作成した
- Ollama の保存先を SD カードへ移動した
- `.zshrc` の `gdircolors` エラーを修正した
- Docker Desktop を起動した
- `docker info` を確認した
- `docker run hello-world` を実行した
- `kubectl` が入っていることを確認した
- `kind` が入っていることを確認した
- `docker build -t study-hello .` を実行した
- `docker run --rm study-hello` を実行した

## Docker / Kubernetes の状態

- Docker Desktop: `docker info` で動作確認済み
- kubectl: インストール済み (`v1.35.3`)
- kind: インストール済み (`v0.31.0`)

## 次にやること

- nginx 演習を実行する
- ポート公開と bind mount を理解する
- `kind` で最初のローカルクラスタを作る

## 疑問

- Docker Desktop と OrbStack のどちらをメインにするか
