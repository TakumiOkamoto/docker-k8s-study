# 2026-04-12

## What I Did

- Created this study repository.
- Moved Ollama model storage to SD card.
- Fixed the `gdircolors` startup error in `.zshrc`.
- Opened Docker Desktop.
- Verified `docker info`.
- Ran `docker run hello-world`.
- Verified `kubectl` is installed.
- Verified `kind` is installed.
- Built the first image with `docker build -t study-hello .`.
- Ran the first custom container with `docker run --rm study-hello`.

## Docker / Kubernetes Status

- Docker Desktop: running and confirmed with `docker info`
- kubectl: installed (`v1.35.3`)
- kind: installed (`v0.31.0`)

## Next Step

- Run the nginx exercise
- Learn port publishing and bind mounts
- Create the first local Kubernetes cluster with `kind`

## Questions

- Do I want Docker Desktop or OrbStack as my main local runtime?
